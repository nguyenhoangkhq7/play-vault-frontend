{
  "compilerOptions": {
    "target": "ES6",
    "module": "ESNext",
    "jsx": "react",
    "allowSyntheticDefaultImports": true,
    "baseUrl": ".",
    // Nếu bạn gặp lỗi này, thử bỏ qua tùy chọn "paths" và "baseUrl" tạm thời.
    "paths": {
      "@/*": ["./src/*"]
    }
  },
  // Chỉnh sửa để bao gồm rõ ràng các file JavaScript/React:
  "include": [
    "./src/**/*.js",
    "./src/**/*.jsx",
    "./src/**/*.ts",
    "./src/**/*.tsx",
    "./public/**/*"
  ],
  "exclude": ["./node_modules", "./dist", "./build"]
}
